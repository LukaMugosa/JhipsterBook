apply plugin: "war"

bootWar {
    mainClassName = "me.amplitudo.store.StoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
